/**
 * Supabase Auth "Confirm signup" email — keep in sync with
 * `supabase/templates/confirmation.html` (applied via config.toml locally;
 * paste into Dashboard → Auth → Email Templates on hosted projects).
 */
export const CONFIRM_SIGNUP_EMAIL_SUBJECT = 'Potwierdź rejestrację w Vestiqo';
